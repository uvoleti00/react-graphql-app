export interface People {
    id: string
    firstName: string
    lastName: string
}

export interface Car {
    id: string
    year: string
    make: string
    model: string
    price: string
    personId: string
}
